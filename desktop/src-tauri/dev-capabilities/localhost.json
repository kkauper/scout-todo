{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "remote-localhost-dev",
  "description": "IPC allowed from the live Scout site: report timer state, listen for tray actions",
  "windows": ["main"],
  "remote": { "urls": ["http://localhost:3000"] },
  "permissions": ["allow-timer-changed", "core:event:allow-listen", "core:event:allow-unlisten"]
}
